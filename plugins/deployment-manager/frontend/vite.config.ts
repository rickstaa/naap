import { createPluginConfig } from '@naap/plugin-build/vite';

export default createPluginConfig({
  name: 'deployment-manager',
  displayName: 'Deployment Manager',
  globalName: 'NaapPluginDeploymentManager',
});
