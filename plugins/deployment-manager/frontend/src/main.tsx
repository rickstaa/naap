import React from 'react';
import ReactDOM from 'react-dom/client';
import { DeploymentManagerApp } from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DeploymentManagerApp />
  </React.StrictMode>,
);
